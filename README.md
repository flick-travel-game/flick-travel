# 世界フリック旅行

フリック入力のタイピングゲーム。**世界の旅**(名所 360か所・36レベル)・**日本の旅**(名所 150か所・15レベル)・**世界の偉人**(100人・10レベル)・**日本の偉人**(100人・10レベル。2026-09-25)・**世界の地名**(国・町・山脈・川・砂漠・海・海峡・半島 350か所・35レベル。2026-09-24 けいくん「大学受験にも対応するレベルで」で 100 → 250 → 350)・**日本の地名**(都道府県・町・平野・山脈・川・湖・半島・海・工業地帯 250か所・25レベル。同日 100 → 250)の 4つ + 世界・日本の一周マップ。

- 2026-09 に 名所だけにした。名言(`QUOTES`)の しくみは `index.html` に 残して、画面に 出していない
- 2026-09-24 に 100か所 → 350か所(世界250・日本100)にして、**世界の旅 / 日本の旅** に分けた(けいくん「もっともっと 世界中の素晴らしい場所を」)。
  はじめの100か所は `index.html` の `SPOTS` に 直接、足した250か所は **`tools/spots-japan.tsv` / `tools/spots-world.tsv`**
  (キー・名前・場所・よみ・Commons の検索語・解説・緯度・経度)に書いて `python3 tools/add_spots.py` で `index.html` に流しこむ(SPOTS-MORE の印のあいだ)。
  **名所を足す・直すときは TSV を直して add_spots.py → fetch_photos.py(写真)→ make_ruby.py(ふりがな)の順**。
  日本かどうかは 場所が「日本」ではじまるかで分ける(`isJapan`)
- 2026-09-25 に **世界遺産検定の受験レベル**まで広げた(けいくん「世界遺産検定の受験対応レベルにしてください」)。日本 +50(=150)・世界 +110(=360)。
  足しかたは 同じ(TSV → add_spots.py → fetch_photos.py → make_ruby.py)。写真が見つからなかった名所は イラストのまま
- **日本一周マップ**(`japan-map-mono.jpg`)は Natural Earth 1:10m の land(パブリックドメイン)から `tools/make_japan_map.py` で作る
  (世界地図と同じ ペン画風。フィルターは `tools/map-defs.svgfrag`)。本土は 東経128.5〜146.5 / 北緯29.8〜46、南西諸島(沖縄・八重山)は 左上の わく。
  ピンの位置は `index.html` の `JAPAN_MAP` / `japanPos` が 同じ数字で計算する。**地図の切りかたを変えたら 両方 直す**。
  作りかた: `ne_10m_land.zip` をほどく → `python3 tools/make_japan_map.py ne_10m_land.shp japan.svg` → ブラウザで 3240px幅に描いて 1080px に縮める
- **世界一周マップ**: クリアしたレベルの名所に ピンが立つ。**やった所だけ 📍。まだの所は 出さない**(2026-09-26 けいくん「赤ピンだと地図が赤くなって 見えなくなってくる」→「やってない問題の白ピンはすべて削除して」→「📍にする?」)。
  **地図は 2本の指(ピンチ)・＋－・ダブルタップ・Ctrl+ホイールで 8倍まで 大きくできる**(同日「もっと地図を拡大出来るようにならないと 目的のピンにたどり着かない」)。
  `.map-in` の **幅**を 倍率ぶん 広げる(📍は px の大きさ・% の位置なので **大きさは 変わらず 場所だけ 離れる**。ブラウザの拡大では 📍も 大きくなって 重なったまま だった)。⚠️ はじめ transform の scale で 作ったら、Safari が 1倍で描いた絵を 引きのばすので 大きい絵に差しかえても ぼやけたままだった。幅で 大きくすれば 描きなおす。しくみは `index.html` の `MZ` / `mapApply` / `mapZoomAt`。地図を切りかえると もどる。
  **1.2倍をこえたら 4320px(`*-4k.jpg`。`MAPS.*.hi`)、3.5倍をこえたら 8640px(`*-8k.jpg`。`hi2`)の地図に 差しかえる**(同日「拡大すると地図がボヤけて見えない」→「もっと拡大しても対応出来るように」)。最大 12倍。はじめは 1080px。大きい絵は scratchpad の `render.mjs` で 同じ SVG(`world-color.svg` / `japan-color.svg`)を 4320 / 8640 幅で描いて JPEG(q80 / q78)にしたもの(8640 の日本は screenshot の timeout を 0 にしないと 落ちる)地図は **`world-map-color.jpg`**(2026-09-24 けいくん「世界フリック旅行みたいな カラフルで綺麗な画像にしたい」→
  `tools/make_color_map.py` で 同じ地図データを 描いた。同日「やっぱり絵は無い方がいい / 地図をボタンと同じカラーを使って キラキラ地図にしたい」→
  **ボタンと同じ色(`TONE` / `COLORS.level` の 9色)で 国を ぬり、海は 世界の旅のボタンの青、ボタンと同じ feTurbulence の キラキラ**を 重ねる。
  かざり(雲・気球など)は 無し。→ 同日「もっと 画像みたいに さわやかな青ベースで カラフルに / 日本も都道府県に線を入れて」で
  **海は うすい空色、国・都道府県は トップの絵の字と同じ あかるいキャンディ9色、白いふち**に。日本は Natural Earth 10m の admin-1(都道府県 47)を 1つずつ ぬる(`japan-map-color.jpg`)。
  となりの国は うすい水色)。
  ⚠️ AI に地図を描かせない(場所が ずれて ピンが合わなくなる)。**切りかたは モノクロ版と同じ**なので ピンの計算は そのまま。
  モノクロ版(`world-map-mono.jpg` / `japan-map-mono.jpg`)は 使っていないが、作りかたの記録として 下に残す。もとの地図は `world-map-mono.jpg`
  (Wikimedia Commons の BlankMap-Equirectangular.svg。CC0。Natural Earth のデータ)を 北緯84度〜南緯60度で 切り、
  SVG のフィルター(紙のきめ・ペンの斜線・ゆらぐ黒い海岸線・海岸にそった波紋の線)で モノクロのペン画風にして 3倍で描いてから 縮めたもの。
  海の名前と方位は HTML の字(手書き風の書体 Yomogi)。
  ピンの位置は `LATLON`(緯度・経度)から 計算する
- 名所の ピン(行った所)と 結果の解説カードに「Googleマップで見る」(登録のいらない ふつうのリンク)。**渡すのは「名前 + 緯度・経度」の 場所リンク**(`/maps/place/<名前>/@緯度,経度,ズーム`。2026-09-25 けいくん「Googleマップ開いたら 現在地に飛んだよ」→ 緯度経度だけにしたら「数字の表示だとどこだかわからないよ」。名前の文字だけだと iPhone のアプリが 検索語を落として 現在地で ひらき、数字だけだと ピンの名前が 数字になる。ズームは 国 5 / 都道府県 8 / 町・山・川 10 / 名所 15)

- **レベルは 打つ文字の多さで決まる**(`index.html` の `cost`)。濁点・小さい字・のばす棒は フリックで1回よけいに押すので 少し重く数える。
  かんたんな順にならべて 10問ずつ区切るので、どのレベルも いつも同じ10問・同じ順番(タイムをくらべられる)
- 自己ベストは レベルごと(ブラウザの localStorage `flick-best-<world|japan>-<レベル>`)。えらんだ旅は `flick-mode`。
  **最後の結果画面は `flick-last`**(2026-09-25 けいくん「戻るボタンで元のゲーム画面に戻らない」。Googleマップから もどると iPhone が ページを作りなおして はじめの画面になっていた。`finish()` が 結果を覚え、ひらいたとき `restoreLast()` が 1時間いないなら 同じ結果画面を出す。「地図にもどる」「もう一度」「次のレベル」で 忘れる)
- 名所の解説には ふりがな(`tools/make_ruby.py` が Sudachi で読みを出して `index.html` の `RUBY` に書く。
  解説の文を変えたら もう一度 走らせる。読みのまちがいは 台本の `FIX` / `SPOT_FIX` で直す)

- **ロゴと トップの絵**(2026-09-24。speed-king の `docs/ロゴの文字.md` の とおり): けいくんが ChatGPT で作った `hero.webp`(1536×1024)の中の
  題名を `tools/logo-word-cut.py` で 切りぬいた `logo-word.webp`(1170×209。**もとの大きさのまま**しまう)+ 左のマークは `logo-mark2.webp`(144×144。2026-09-25 けいくん「この左上のアイコンも変更して」で、ホーム画面のアイコンと同じ 正方形の絵に。まえは 絵の地球儀を切ったもの)。
  画面では どちらも **44px**。⚠️ **CSS・フォントで 似せない**。絵を変えたら 道具を走らせなおす(`FRAC` と `parts[:7]` = 7文字)。
  名前は `<h1 class="sr-only">` でも 置いてある(読みあげ・検索のため)

- **偉人バージョン**(2026-09-25 けいくん「世界の偉人、日本の偉人。結果がでたら偉人の簡単なプロフィールと名言の解説つきで紹介」): `tools/people-world.tsv` / `people-japan.tsv`(100人ずつ。**亡くなっている人だけ**)。
  12列 = key・名前・国と時代・よみ・**Wikipedia の題名(肖像を取る)**・プロフィール・緯度・経度(**生まれた所**)・名言・名言の解説・はじめのレベル(1)・生まれた所の名前。
  `add_spots.py` が `k:"person", s:名言, sd:解説, b:生まれた所` を付け、`POOLS.wpeople / jpeople`。打つのは 名前。結果カードに プロフィール → 名言 → 解説(`quoteBlock`)、
  Googleマップは 生まれた所(ズーム10)、ピンも 生まれた所。ふりがなは `RUBY[key]`(プロフィール)/ `RUBY[key+"|s"]`(名言)/ `RUBY[key+"|sd"]`(解説)。
  **肖像は `fetch_photos.py` が Wikipedia(日本語版→英語版)の 記事の代表画像(pageimages)を Commons から取る**(`lead_image`。`tools/people-portraits.json` に おぼえる)。
  偉人のキー(`PEOPLE`)だけ 白黒・たて長・400px以上 を通す(古い肖像写真のため)。名言は 出どころが はっきりしているものだけ。作り話(アントワネットの「お菓子」など)は 解説に そう書く
- **だれが あそぶ？(6人まで)**(2026-09-25 けいくん「かずとものように右上にアイコンを作って 6人まで登録出来るように」→ 同日「違う かずともと まったく同じにしてください」):
  **かずとも(speed-king の `Game.tsx` の picker)と 同じ形**。右上 = うすい灰色の なまえ + 44px の 四角い タイル(写真 か なまえの 1文字。色は 人の順番で `TONE_LIST`)。
  ひらくと 白いカードに「だれが あそぶ？」→ 4列の 60px タイル(えらんだ人は 黒いわく・ほかは うすく)+「＋ ふやす」→
  なまえの欄(8文字。かずとも `nickname.ts` と同じ `checkNickname`: きずつける言葉・学校名・数字4つ以上・れんらくさきは 断る。打ちながら 赤い字で出る)→
  いま えらんでいる人(📷 で 写真 → `toIconDataUrl` が 160px の四角・JPEG 0.8 にして `flick-photo-<id>` に。「写真を もどす」)→「なまえを なおす」「この人を 消す」(**記録は 消さない**)→
  せってい「だれと くらべる？」= **ぜんいん / 学年の えらぶ欄** の 2つだけ(`flick-bygrade`。学年を さわると その人の学年も 変わる)。⚠️ えらぶ欄は iPhone の 素の見た目(白い線・小さい字)が 出るので `border:none` `font` `text-align-last:center` を 自分で書く。
  ⚠️ **最後の結果(`flick-last`)を もどすのは「もどる」と「再読み込み」のときだけ**(`performance.getEntriesByType("navigation")[0].type`)。リンク・ホーム画面から ひらいたときは トップ(けいくん 2026-09-26「リンクからトップ画面ではなく終わった後のページに飛ぶ」)。
  端末の中だけ(`flick-profiles` / `flick-profile`)。**自己ベストとピンは 人ごと**(`flick-best-p<id>-<旅>-<レベル>`。`bestKey()` の1か所)。
  だれも登録していないときは いままでどおり `flick-best-<旅>-<レベル>`。**はじめて登録した人に それまでの記録を引きつぐ**。新しい人の学年は いまの人の学年を 引きつぐ。写真は どこにも送らない
- **こたえの名前に ふりがな**(同日): 結果カードの見出しと ピンの説明(`nameRuby()`。よみは 打つ字 `q.r`)
- **地名の Googleマップは「説明に出てくる有名な場所」へ**(同日「説明に書いてある有名な場所にGoogleマップ飛んだ方がいい」): `names-*.tsv` の **9列目「地図の目印」**(都道府県47・国149に入れた。町・山・川は 空 = その地名そのもの)。
  `add_spots.py` が `m:"伊勢神宮 内宮"` を付け、`gmapUrl` が `目印 + 地名` で ズーム13
- **みんなのランキング(学年ごと)**(2026-09-25 けいくん「スピードランキングも かずとものように 学年順で競走出来るように」→「1」):
  このゲームは サーバーを持たないので、**かずとも(kazutomo.app)の API が 代わりに 受けつけ・読み出し**をする
  (speed-king の `/api/flick/score` と `/api/flick/board`。表は かずともの Supabase の `flick_scores`。SQL は speed-king の `docs/db/013-flick-scores.sql`)。
  ここには 鍵も 表も 無い(`RANK_API` の 住所だけ)。10問 打ち終わると、右上で なまえを 入れた人なら 記録を送り(`submitScore`)、結果画面の
  「🏆 みんなのランキング」に 速い順 20人(きょう / 今週 / いままで)。せっていで「ぜんいん」なら `grade=all`、学年なら その学年だけ。なまえを 入れていない人には「なまえを 入れると くらべられるよ」。
  なまえの決まり(きずつける言葉・学校名・電話番号・連絡先は 使えない)と 速すぎチェック(1問 0.25秒より速い回は のらない)は かずともと同じ。
  つながらないときも あそびは 止めない(箱に「いまは 読めません」と出るだけ)。学年の しるしは `k / e1〜e6 / j1〜j3 / h / adult`(`GRADES`)
- **地名バージョン**(2026-09-24 けいくん「地名バージョンも 世界と日本の作成してください」): `tools/names-world.tsv` / `names-japan.tsv`。
  5列目は 写真の検索語ではなく **絵文字**(国旗など)で、カードには 写真のかわりに 絵文字が出る(`pic()` の `q.e`)。
  `add_spots.py` が `k:"name", e:"🇫🇷"` を付けて index.html に書き、`POOLS.wnames / jnames` に分かれる。
  **国(国旗の絵文字)と 都道府県には `f:1`** が付き、レベルは **その人たちが先**(その中で かんたんな順)→ そのあと 町・山・川(けいくん 2026-09-25「まずは世界の国名や 日本は県から問題がはじまると良い」)。決めかたは `add_spots.py` の `first_of`。**名所の旅も 同じ**: `index.html` の はじめの100か所(定番)に `SPOTS.forEach(q => q.f = 1)` で印を付け、世界遺産検定レベルの名所は そのあと(同日「1でお願いします」)。地図・ピン・ふりがな・Googleマップは 名所と同じしくみ

## ファイル

| ファイル | 中身 |
|---|---|
| `index.html` | ゲーム本体(遊びかた・デザイン) |
| `about.html` / `lp-*.webp` | 紹介ページ(2026-09-25 けいくん「販売ページを作成してください」)。おうちの方むけに、4つの旅・あそびかた・守っていること・よくある質問。写真は 手元のゲームを Playwright で自動で遊ばせて撮ったもの(scratchpad の `lp-shots.mjs`)。⚠️ 無料・登録なし・広告なし と書いてある。お金を取る形にするときは ここも書きかえる。ゲームの いちばん下から リンク |
| `hero.webp` / `logo-word.webp` / `logo-mark2.webp` | トップの絵と そこから切った ロゴの字、左のマーク(ホーム画面のアイコンと同じ絵)(`tools/logo-word-cut.py`) |
| `apple-touch-icon.png` / `icon-512.png` / `favicon.png` / `manifest.webmanifest` | ホーム画面に追加したときのアイコンと名前(ゲーム=「世界フリック旅行ゲーム」/ 紹介ページ=「世界フリック旅行」。けいくん 2026-09-25。⚠️ 紹介ページには manifest を付けない — 付けると start_url のゲームが ひらいてしまう)(2026-09-25 けいくん「左上の画像に」。同日 けいくんの正方形の絵(1254px)に差しかえ。いま `?v=2`)と アプリのように開く設定。絵を差しかえたら `?v=` を上げる |
| `world-map-color.jpg` / `japan-map-color.jpg` | いま使っている カラフルな地図(`tools/make_color_map.py`) |
| `world-map-mono.jpg` | まえの モノクロのペン画風の地図(もとは CC0)。いまは使っていない |
| `japan-map-mono.jpg` | 日本一周マップ(もとは Natural Earth。パブリックドメイン) |
| `tools/spots-*.tsv` | 足した名所の一覧(ここを直す)。`tools/add_spots.py` で index.html に流しこむ |
| `tools/make_japan_map.py` | 日本一周マップを作る台本 |
| `photos.js` | 名所の写真の一覧(撮影者・ライセンス・元のページ) |
| `photo-*.jpg` | 名所の写真(幅800pxに縮小ずみ。iPhone からでも上げやすいよう フォルダに入れない) |
| `tools/fetch_photos.py` | 写真を Wikimedia Commons から取ってくる台本 |
| `.nojekyll` | GitHub Pages に「そのまま出して」と伝える空のファイル |

写真が無い場所は、いままでどおりイラストが出る。

## 写真のきまり

- 使うのは **パブリックドメイン(CC0 をふくむ)/ CC BY / CC BY-SA** だけ。
  `tools/fetch_photos.py` が Commons の API でライセンスを確かめて、ほかは とばす
- 結果画面の紹介カードに「写真：撮影者（ライセンス名）／Wikimedia Commons」を出す。
  撮影者名は元のページへ、ライセンス名はライセンスの本文へのリンク
- エッフェル塔は **昼の写真だけ**(夜のライトアップには著作権がある)
- ⚠️ Commons の API が「Public domain」と返しても、ページに `{{Licensed-PD}}` とあれば
  **写っている物(塔・像)がパブリックドメインなだけで、写真は別のライセンス**。
  台本はこれを見て 写真のライセンス(エッフェル塔・自由の女神は CC BY-SA 3.0)を書く
- Commons の写真は **決まった大きさ(960px など)の縮小版しか取れない**(https://w.wiki/GHai)。
  台本は 幅が960pxより大きい写真だけを選ぶ

写真を入れなおすとき(flick-travel/ で):

```
pip install Pillow
python3 tools/fetch_photos.py            # 100か所ぜんぶ(時間がかかる)
python3 tools/fetch_photos.py fuji petra # 一部だけ
```

## GitHub Pages で公開する手順

GitHub Pages は リポジトリの「いちばん上」か「docs フォルダ」しか公開できない。
なので **このゲーム用の新しいリポジトリ**を作って、このフォルダの中身をそこに置く。

1. https://github.com/new をひらく
2. Repository name に `flick-travel`、**Public** をえらんで「Create repository」
3. できたページの「uploading an existing file」を押す
4. このフォルダの中身(`index.html`・`photos.js`・`*-map-mono.jpg`・`photo-*.jpg`・`.nojekyll`)を
   まとめてドラッグして、いちばん下の「Commit changes」。⚠️ **1回に上げられるのは 100ファイルまで**。写真は 100枚ずつに分けて 何回かに
5. リポジトリの **Settings → Pages** をひらく
6. 「Build and deployment」の Source を **Deploy from a branch**、
   Branch を **main** と **/ (root)** にして「Save」
7. 1〜2分まつと、同じ画面の上に URL が出る
   → `https://<GitHubのユーザー名>.github.io/flick-travel/`

写真を差しかえたら、同じリポジトリに もう一度アップロードするだけで 数分で反映される。

- **エアーズロック(ウルル)**: こたえは「えあーずろっく」(2026-09-26 けいくん「観光名所と考えるとエアーズロックの方を答えにした方が良くない?」)。名前は「エアーズロック（ウルル）」。key は `uluru` のまま(写真・ピン・ふりがなの ひもづけを 変えないため)

## 名所は 有名な順(けいくん 2026-09-26「観光名所を世界ランキング順に並べてもらえると 有名な知っている場所がたくさん出てきて良い / 日本の観光名所も同じように」)
- **世界の旅・日本の旅の レベルは 有名な順**。有名さは **日本語版 Wikipedia の 閲覧数(直近60日)**(`tools/fetch_fame.py` → `tools/fame.json` と `index.html` の `FAME`)。
  `python3 tools/fetch_fame.py` で 取りなおせる(記事の題名は 名前から引いて 転送をたどる。引けないものは `MANUAL` に 手で書く。いま 引けないのは ホワイトヘブンビーチ・曲阜の孔廟の2つ = いちばん後ろ)
- ⚠️ **閲覧数だけでは 並べない**。「カルタゴ」「バイカル湖」「モナコ」のように 町・湖の記事は 名所より よく読まれるので、
  **はじめの100か所(`f:1` = 定番)を 先に**、その中と そのあとを 閲覧数の多い順にした(`LEVELS` の sort)。地名・偉人の並びは 変えていない
- ⚠️ wikimedia.org の pageviews API(1記事ずつ)は 429 が 1分おきに出て 使いものにならなかった。Wikipedia の `prop=pageviews`(10記事ずつ + continue)を使う。
  **50記事まとめて 1回**だと 一部しか返ってこず 271か所が 0 になった(continue を たどらないと だめ)
- **有名なほうを こたえにした**(同日「エアーズロックのように 有名な方が答えになっていない問題をすべて直して」): ユングフラウ(ヨッホ)/ イスファハン(イマーム広場)/
  マラケシュ(ジャマ・エル・フナ広場)/ サマルカンド(レギスタン広場)/ 佐渡金山(佐渡島の金山)/ 十二使徒(12使徒)。key は 変えていない
- **同じ名所の 2つめだった 4か所を 差しかえた**(彦根城天守・端島炭坑・姫路城西の丸・法隆寺夢殿 → **白鬚神社・九十九島・神戸ポートタワー・奈良公園**)。
  こたえが 同じ(ぐんかんじま)になる うえ、閲覧数で 並べると 同じ記事なので となりに 来てしまうため。有馬温泉・琵琶湖(地名モードに もうある)・ハウステンボスは やめた

## きろく(のび の グラフ・トップの ランキング・メダル)(けいくん 2026-09-26「いままでの記録をかずとものようにグラフ表示 / かずともとまったく同じ表示で / 1位〜3位も メダル表示に」)
- **1回ごとの タイムを 端末に しまう**ようにした(`flick-hist-p<id>`。だれも登録していなければ `flick-hist`。新しい順に 400回まで。`pushHist` / `hist` / `growthOf`)。
  それまでは 自己ベストしか 残していなかったので、**グラフは ここから先の 記録だけ**(前の自己ベストは 数字として 残っている)
- **のび**(`growthHtml`)は かずとも `src/components/Growth.tsx` の GrowthChart と 同じ絵: SVG 320×128・上にいくほど 速い・ベストの点線・うすい面・👑(いちばん速い回に 1つ)・
  はじめ/いま・「🎉 ◯びょう 速くなった！」・「◯回 あそんだ ・ ベスト」・さいきんの 12回。タイムの字も かずともと同じ「36.7びょう」「1分10.0びょう」(`fmtT`)。
  1回だけのときは「まだ 1回だけ(…)。もう1回 あそぶと ここに のびが 出ます」。色は その旅の色(`COLORS.mode`)
- 出る場所は 2つ(かずともと同じ): **結果画面**(自己ベストの数字の下。2回め から)/ **トップの「きろく」**(レベルの一覧の下。あそんだ人にだけ)。
  トップは いまの旅で あそんだ レベルの ふだ(`#rec-lv`)を えらぶと、その レベルの **🏆 みんなのランキング**(`drawBoard(m, lv, "board2")`)と **のび** が 出る。
  ⚠️ `drawBoard` は 入れものの id を 受けとる(`board`=結果 / `board2`=トップ)。読んでいるあいだに レベルを 変えたら 古い返事は 捨てる
- **1〜3位は 🥇🥈🥉**(`MEDALS`。4位からは「4位」)。ランキングの ならびは いままでどおり

## Googleマップは その場所の カードが ひらく形(けいくん 2026-09-26「直接この姫路城に飛べるように / すべての答えも」)
- `gmapUrl` は **`/maps/search/<名前>/@緯度,経度,ズーム`**。まえの `/maps/place/<名前>/@座標` は 座標に 赤いピンが 立つだけで、
  姫路城の カード(評価・写真・営業時間)が ひらかなかった。search なら その場所を さがして カードが ひらく。座標は さがす目安
- 名前の かっこの中(「（ウルル）」)は 外して さがす(`plain`)。偉人は 生まれた所、地名は 目印(`m`)+地名 のまま
- **旅のカードを押すと レベルの一覧まで 画面が送られる**(2026-09-26 けいくん「選んだらその問題に飛ぶように」)。地図が あいだにあって レベルが 見えなかったため。`drawModes` の click で `#lv-title` へ `scrollIntoView`

## 受験むけの 3つの旅: 世界の首都・世界史・日本史(けいくん 2026-09-26「大学受験、高校受験に役立つやつ」→「おすすめすべてお願いします」)
- **世界の首都**(`capitals`。195か国 = 国連加盟193 + バチカン + 台湾。19レベル)/ **世界史**(`wevents`。120の出来事・12レベル)/ **日本史**(`jevents`。120の出来事・12レベル)。
  データは `tools/capitals.tsv` / `events-world.tsv` / `events-japan.tsv`(10列: key・名前・場所・よみ・絵文字・解説・緯度・経度・地図の目印・はじめのレベル)。
  形は `tools/check_extra_tsv.py <tsv>` で確かめる(列の数・よみ・key のかぶり・解説の長さ)。`add_spots.py` が `k:"capital"` / `k:"event"` + `e`(絵文字)+ `m`(目印)+ `f:1` を付ける
- 首都は 国名(`c`)を見て 首都名を打つ。カードは 国旗の絵文字(写真は無い)。出来事は 名前を打ち、カードに「1789年・フランス」と 解説。ピンは 起きた場所、Googleマップは 目印(`m`)へ
- **レベルの順は 地名と同じ**: 中学で習うもの(`はじめのレベル`=1。首都 60・世界史 40・日本史 47)を 先に、その中で かんたんな順 → そのあと 大学受験レベル
- 画面の言葉は `MAPS.*` の `unit`(か国 / 件)・`doneWord`(おぼえた国 / 知った出来事)・`miss` で 切りかえる(`unitOf`)。カードの色は `COLORS.mode`(⚠️ `toneFrom` は 明るい色だと 字が 暗くなる。首都の色は 白い字になる こさに した)
- **ランキング**: かずとも側は `FLICK_MODES` に 3つ 足した(speed-king PR #164)。⚠️ 表の check `flick_mode_ok` が 6つ決め打ちなので `docs/db/014-flick-modes.sql` を けいくんが 流すまで、新しい旅の記録は 表に入らない(ゲームは「この回は ランキングに のりませんでした」と出るだけ)
- ⚠️ 首都の担当エージェントは 1回めに 止まっていた(けいくんの 割りこみで 消えた)。**エージェントに 書かせたら ファイルが できているか 自分で確かめる**

## れんぞく(つづけて あそんだ日)(けいくん 2026-09-26「連続ゲーム記録を実装してください」)
- かずとも `src/components/Streak.tsx`(StreakCard / StreakBoard)と `scores.ts` の `streakOfDays` と **同じ 数えかた・同じ 見た目**。
  旅の しゅるいも レベルも かけない(あそんだ日は あそんだ日)。スマホの時計で 0時に 日が変わる。**きのうまで つづいていれば まだ 生きている**
- ⚠️ **切れても 責めない**: 0日のときは「きょう あそぶと 1日目」。「とぎれました」とは 書かない。きのうまでの人には「きょう あそぶと ◯日目」、いまが最長なら「⭐️ いまが さいこう記録！」
- あそんだ日は 人ごとに `flick-days-p<id>`(日の通し番号。400回で 消える 履歴 `flick-hist` とは べつに ずっと残す。はじめは 履歴から作る)。`pushHist` が 足す
- 出る場所: **トップの「きろく」**(ランキングと のびの あいだ。どの旅でも 1回でも あそんでいれば 出る)/ **結果画面**(自己ベストの数字の下)
- **みんなの れんぞく**: かずともの `GET /api/flick/streak`(speed-king PR #165)。さいきん60日の記録から なまえごとに 数えて 長い順に10人。自分の行は 色つき、1〜3位は メダル。つながらないときは 何も出さない
- ⚠️ 履歴は 2026-09-26 から しまいはじめたので、れんぞくも そこから数える

## フリック旅行シリーズ(けいくん 2026-09-26「世界フリック旅行 / 歴史フリック旅行 … このゲームすべて作成してください」)
- **1つの土台(index.html)から、シリーズの ほかのゲームの ページを 作る**: `python3 tools/build_games.py` → `rekishi/index.html`(歴史。あとで `uchu/` `karada/`)。
  ⚠️ **直すのは index.html だけ**。走らせれば ほかのゲームにも 同じ直しが入る。できた ページは 手で直さない(次に 走らせると 消える)
- どのゲームかは 土台の いちばん上の `GAME`(id・名前・出す旅 modes・写真の道 assets・ロゴ/絵の有無・文)。`MODES` は `GAME.modes` で しぼる。
  `SERIES` が いちばん下の「🧭 フリック旅行シリーズ」のリンク(いま見ている ゲーム以外)
- **世界フリック旅行** = 世界の旅・日本の旅・世界の地名・日本の地名・世界の首都(1,305問)。**歴史フリック旅行** = 世界の偉人・日本の偉人・世界史・日本史(440問)。
  同じ `flick-travel-game.github.io` の下なので、**なまえ・写真・れんぞく・自己ベスト(localStorage)は 2つで 共通**。前に 偉人・歴史で出した 自己ベストは そのまま 歴史のほうに出る
- **歴史は 地図ではなく 年表**(`MAPS.*.time`。`drawTimeline`)。年 → 横の位置は 折れ線(`TIME_SEG`。むかしほど ぎゅっと、近代ほど ゆったり)。
  時代の帯(`ERAS`: 日本=縄文〜令和 / 世界=古代〜現代)。📍は 出来事の年・偉人の 生まれた年。同じ年に かたまるときは 5段に分ける。拡大は 地図と同じ しくみ
- **偉人の 生没年は Wikidata から**(`tools/fetch_years.py` → `tools/people-years.json`。`add_spots.py` が `y0` `y1` として 書く)。出来事の年は 場所の欄「1789年・フランス」「前431〜」「14〜16世紀」から `yearOf` が読む
- 歴史の ロゴ・絵は まだ無い(`logo:false` `hero:false` → 文字の題名)。けいくんの絵が届いたら `rekishi/` に 置いて `build_games.py` の GAMES を `logo:true` に
- ⚠️ 新しい リポジトリは この入れものからは 作れない(GitHub の権限が このリポジトリだけ)ので、**同じ リポジトリの フォルダ**(`/flick-travel/rekishi/`)で 出す

## 宇宙フリック旅行・からだフリック旅行(2026-09-26。同じ日に 4つ そろった)
- `uchu/`(太陽系 102・星と星座 150・宇宙のことば 57 = 309問)/ `karada/`(ほねと きんにく 75・臓器と しくみ 174・細胞と 遺伝 55 = 304問)。データは `tools/space.tsv` / `tools/body.tsv`(11列。`tools/check_series_tsv.py` で確かめる)
- **地図のかわりに SVG の図**(`SVG_MAPS`: solar 太陽系 / sky 星図(赤経・赤緯)/ front からだ / head 頭 / cell 細胞)。⚠️ 図は コードで描く(AI の絵は 使わない。位置が ずれるため)。
  旅によっては 図が 2つあり(`MAPS.*.maps`)、地図の上の タブで 切りかえる(`curSub`)。📍の位置は TSV の x,y(`SVG_MAPS[].pos`)
- カードは 絵文字(写真は まだ無い。Wikipedia の題名 `w` を持っているので、あとで `fetch_photos.py` の pageimages の道で 取れる)。「📖 Wikipedia で くわしく」の リンク
- ⚠️ からだの図は ぼくが 描いた かんたんな 人のかたち。けいくんの絵に 差しかえるなら、**同じ 前向き・同じ位置**(頭 y0.09・肩 y0.22・へそ y0.47・ひざ y0.74)で 描いてもらい `SVG_MAPS.front.bg` を `<image>` に する

## おうちの方の アカウントと つなぐ(かずとも ④-c と 同じ線。2026-09-26)
- おうちの方が かずとも に ログインして **https://kazutomo.app/flick-link** を ひらくと、`https://flick-travel-game.github.io/flick-travel/?link=<しるし>`(歴史・宇宙・からだは `/flick-travel/<rekishi|uchu|karada>/?link=…`)に もどってくる。
  ゲームは しるしを `localStorage` の **`flick-link`** に しまい(同じ 住所の下なので **4つの ゲームで 共通**)、`history.replaceState` で URL から `link` を 消し、1回だけ「おうちの方の アカウントと つながりました」と 出す(`takeLink`)
- できることは **`GET https://kazutomo.app/api/flick/plan`**(しるしが あれば `Authorization: Bearer <しるし>`)で 聞く → `{graph, listed, signedIn}`。`fetchPlan` が メモリと sessionStorage(`flick-plan`。10分)に おぼえる。
  ⚠️ **つながらないときは 責めない**: しるしが あれば ぜんぶ できる(`graph:true, listed:true, signedIn:true`)、無ければ `graph:true, listed:false, signedIn:false` と みなす
- **ランキング**: `POST /api/flick/score` にも 同じ Bearer を 付ける。返事が `listed:false` なら 記録は しまわれたが 名前は のらないので、結果画面の ランキングの下に
  「この回は ランキングに 名前が のりませんでした。おうちの方が アカウントを つなぐと のります」(もとの 失敗の 字「この回は ランキングに のりませんでした: …」「いまは ランキングを 読めません」は そのまま)
- **のび**: `graph:false` のときは **消さずに ぼかす**(`planGrowth`。かずとも `Blurred` と 同じ `filter:blur(5px);opacity:.7;pointer-events:none` + `aria-hidden`)。下に「おうちの方の がめんから つづきが 見られます」。結果画面・トップの きろく の 両方
- **つないでいない人の きろくは ゲームを とじると きえる**(かずとも ④-c②): 履歴 `flick-hist*`・あそんだ日 `flick-days*`・自己ベスト `flick-best-*` は、つないでいない あいだ **sessionStorage** に しまう(`recStore` / `recGet` / `recSet`)。
  2回め から 結果画面に「この きろくは、ゲームを とじると きえます。おうちの方が アカウントを つなぐと のこります」+「おうちの方へ」(→ kazutomo.app/flick-link。歴史・宇宙・からだは `?to=<GAME.id>`)
  - ⚠️ **もう 端末に きろくが ある人は そのまま**: いまの人の `flick-best-`/`flick-hist`/`flick-days` が localStorage に 1つでも あれば、その人は ずっと localStorage(`recPersistent`)。消さない・うつさない
  - つないだら(しるしが 入ったとき・plan が つながっていると 言ったとき)、sessionStorage の きろくを localStorage に **足す**(`keepSessionRecords`。ベストは 速いほう・履歴は かさねて 400回まで・日は 合わせる)。localStorage の きろくは 消さない
  - はじめて なまえを 入れたときの 自己ベストの 引きつぎも、sessionStorage の ぶんは sessionStorage の中で 引きつぐ
- **だれが あそぶ？** の ⚠️ ランキングの 1行の下に: つながっていれば「✅ おうちの方の アカウントと つながっています」+「つなぐのを やめる」(たしかめてから しるしだけ 消す。きろくは 消さない)、
  つないでいなければ「おうちの方の アカウントと つなぐと、きろくが のこり ランキングに 名前が のります」+「おうちの方へ」
- ⚠️ 子どもが 見る 字には お金の ことばを 書かない。「おうちの方へ」の リンクは 同じ タブで ひらく(sessionStorage の この回の きろくを もったまま もどってこられるように)
- 確かめかた: Playwright で kazutomo.app を `page.route` で まねて、しるしなし / ?link=(graph:false)/ しるしあり(listed:true)/ もう きろくが ある人 / つながらない の 5とおり
